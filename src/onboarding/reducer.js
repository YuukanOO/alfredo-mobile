import * as t from './actionTypes';

const initialState = {
  
};

export default function onboardingReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
