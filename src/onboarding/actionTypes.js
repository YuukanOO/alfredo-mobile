import { NAME } from './constants';
import base from './../base';

export const START_ONBOARDING = `${NAME}/START_ONBOARDING`;
export const CONNECT_TO_SERVER = base.actionTypes.createActionTypes(`${NAME}/CONNECT_TO_SERVER`);
